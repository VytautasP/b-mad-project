# Story 2.7: Active Timer UI

## Status

Ready for Review

## Story

**As a** user,
**I want** to start, pause, and stop a timer on tasks,
**so that** I can track time as I work without manually entering minutes.

## Acceptance Criteria

1. "Start Timer" button added to task list view and task detail panel
2. Clicking "Start Timer" initiates browser-based timer showing elapsed time (HH:MM:SS format)
3. Timer displays prominently in UI (sticky header or floating widget) showing active task name
4. Timer updates every second without performance issues
5. "Pause" button allows pausing timer without losing elapsed time
6. "Resume" button continues timer from paused elapsed time
7. "Stop" button ends timer and prompts to save time entry with optional note
8. Saving timer creates TimeEntry via API with EntryType=Timer and calculated minutes (rounded up to nearest minute)
9. Only one timer can run at a time (starting new timer stops current timer with confirmation)
10. Timer state persists in browser localStorage (survives page refresh during active timing)
11. Timer displays even when navigating to different pages within application
12. Successfully saved time entry updates task's LoggedMinutes display immediately
13. Timer shows visual indicator (pulsing dot, color change) when active

## Tasks / Subtasks

- [x] **Task 1: Create Timer State Service** (AC: 4, 10, 11)
  - [x] Create `timer-state.service.ts` in `src/app/core/services/state/`
  - [x] Inject Angular `inject()` function for dependencies
  - [x] Create interface `TimerState`:
    - [x] Property: `isRunning: boolean`
    - [x] Property: `isPaused: boolean`
    - [x] Property: `taskId: string | null`
    - [x] Property: `taskName: string | null`
    - [x] Property: `elapsedSeconds: number`
    - [x] Property: `startTime: number | null` (timestamp)
  - [x] Create private `BehaviorSubject<TimerState>` with initial state
  - [x] Create public `timer$: Observable<TimerState>` from BehaviorSubject
  - [x] Method: `startTimer(taskId: string, taskName: string): void`
    - [x] Save current state to localStorage
    - [x] Update BehaviorSubject with running state
    - [x] Start interval timer (1 second)
  - [x] Method: `pauseTimer(): void`
    - [x] Update state to paused
    - [x] Stop interval
    - [x] Save state to localStorage
  - [x] Method: `resumeTimer(): void`
    - [x] Update state to running
    - [x] Resume interval timer
    - [x] Save state to localStorage
  - [x] Method: `stopTimer(): number` (returns elapsed minutes)
    - [x] Calculate total elapsed minutes (rounded up)
    - [x] Clear timer state
    - [x] Remove from localStorage
    - [x] Return elapsed minutes
  - [x] Method: `loadTimerFromStorage(): void` (called in constructor)
    - [x] Read from localStorage key 'taskflow_timer'
    - [x] If valid timer state exists, restore and resume
  - [x] Private method: `tick(): void`
    - [x] Increment elapsedSeconds
    - [x] Update BehaviorSubject
  - [x] Register service as `providedIn: 'root'`

- [x] **Task 2: Create Timer Widget Component** (AC: 2, 3, 4, 5, 6, 7, 13)
  - [x] Create `timer-widget` component in `src/app/shared/components/`
  - [x] Component files:
    - [x] `timer-widget.component.ts`
    - [x] `timer-widget.component.html`
    - [x] `timer-widget.component.scss`
    - [x] `timer-widget.component.spec.ts`
  - [x] Inject `TimerStateService`
  - [x] Inject `TimeTrackingService` (for saving time entry)
  - [x] Inject `MatDialog` (for stop confirmation)
  - [x] Subscribe to `timerState.timer$` in component
  - [x] Create computed signal or pipe for formatted time (HH:MM:SS)
  - [x] Template structure:
    - [x] Container div with fixed/sticky positioning (top-right corner)
    - [x] Show widget only when `timerState.isRunning || timerState.isPaused`
    - [x] Display task name
    - [x] Display formatted elapsed time (HH:MM:SS)
    - [x] Pulsing dot indicator when running (CSS animation)
    - [x] Button: "Pause" (shown when running)
    - [x] Button: "Resume" (shown when paused)
    - [x] Button: "Stop" (always shown)
  - [x] Method: `onPause(): void`
    - [x] Call `timerState.pauseTimer()`
  - [x] Method: `onResume(): void`
    - [x] Call `timerState.resumeTimer()`
  - [x] Method: `onStop(): void`
    - [x] Open dialog to enter note (optional)
    - [x] Get elapsed minutes from `timerState.stopTimer()`
    - [x] Call `timeTrackingService.logTime()` with EntryType=Timer
    - [x] Show success notification
    - [x] Refresh task data to update LoggedMinutes
  - [x] Styling (SCSS):
    - [x] Fixed position: top-right corner, z-index high
    - [x] Card-like appearance with shadow
    - [x] Pulsing animation for active indicator
    - [x] Responsive design (collapse on mobile)
    - [x] Use Angular Material colors/theming

- [x] **Task 3: Create Stop Timer Dialog Component** (AC: 7)
  - [x] Create `stop-timer-dialog` component in `src/app/shared/components/`
  - [x] Component files:
    - [x] `stop-timer-dialog.component.ts`
    - [x] `stop-timer-dialog.component.html`
    - [x] `stop-timer-dialog.component.scss`
  - [x] Use `MAT_DIALOG_DATA` to receive elapsed minutes
  - [x] Template:
    - [x] Display elapsed time (formatted as "2h 30m")
    - [x] Textarea for optional note (maxlength 500)
    - [x] "Save" button (primary)
    - [x] "Cancel" button
  - [x] Method: `onSave(): void`
    - [x] Return dialog data: `{ note: string, confirmed: true }`
  - [x] Method: `onCancel(): void`
    - [x] Close dialog with `{ confirmed: false }`

- [x] **Task 4: Add Start Timer Button to Task List** (AC: 1, 9)
  - [x] Update `task-list.component.ts`:
    - [x] Inject `TimerStateService`
    - [x] Inject `MatDialog` for confirmation
    - [x] Subscribe to `timerState.timer$` to get current timer state
  - [x] Update `task-list.component.html`:
    - [x] Add "Start Timer" button/icon to each task row
    - [x] Disable button if timer is already running for different task
    - [x] Show "Timer Running" badge if this task's timer is active
  - [x] Method: `onStartTimer(task: Task): void`
    - [x] If another timer is running, show confirmation dialog
    - [x] If confirmed, stop current timer with auto-save
    - [x] Call `timerState.startTimer(task.id, task.name)`
    - [x] Show success notification

- [x] **Task 5: Add Start Timer Button to Task Detail Panel** (AC: 1, 9)
  - [x] Update `task-detail.component.ts`:
    - [x] Inject `TimerStateService`
    - [x] Subscribe to `timerState.timer$`
  - [x] Update `task-detail.component.html`:
    - [x] Add "Start Timer" button in task actions area
    - [x] Disable if timer running for different task
    - [x] Show "Timer Running" badge if active for this task
  - [x] Method: `onStartTimer(): void`
    - [x] Same logic as Task 4 (extract to shared method if needed)
    - [x] Handle confirmation for switching timers

- [x] **Task 6: Add Timer Widget to App Root** (AC: 11)
  - [x] Update `app.component.html`:
    - [x] Add `<app-timer-widget></app-timer-widget>` at root level
    - [x] Widget will display globally across all pages
  - [x] Import `TimerWidgetComponent` in `app.component.ts`

- [x] **Task 7: Update Time Tracking Service** (AC: 8)
  - [x] Update `time-tracking.service.ts` in `src/app/core/services/api/`:
    - [x] Ensure `logTime()` method accepts `EntryType` parameter
    - [x] Method signature: `logTime(taskId: string, minutes: number, note: string, entryType: 'Timer' | 'Manual'): Observable<TimeEntry>`
    - [x] POST to `/api/tasks/${taskId}/timeentries` with DTO including EntryType

- [x] **Task 8: Update Task State After Timer Save** (AC: 12)
  - [x] In `timer-widget.component.ts` `onStop()` method:
    - [x] After successful time entry save, emit event or call task state service
    - [x] Trigger refresh of task data to update `TotalLoggedMinutes`
  - [x] Option 1: Call `taskStateService.refreshTask(taskId)`
  - [x] Option 2: Emit custom event that task-list/task-detail listens to

- [x] **Task 9: Add localStorage Persistence Logic** (AC: 10)
  - [x] In `timer-state.service.ts`:
    - [x] Define localStorage key: `TIMER_STORAGE_KEY = 'taskflow_timer'`
    - [x] Method: `private saveToStorage(state: TimerState): void`
      - [x] Serialize state to JSON
      - [x] Store in localStorage
    - [x] Method: `private loadFromStorage(): TimerState | null`
      - [x] Read from localStorage
      - [x] Validate structure
      - [x] Return parsed state or null
    - [x] Call `loadFromStorage()` in constructor to restore timer on app load
    - [x] Resume interval if timer was running before refresh

- [x] **Task 10: Create Confirmation Dialog for Timer Switch** (AC: 9)
  - [x] Create `confirm-timer-switch-dialog` component or reuse generic confirmation dialog
  - [x] Template:
    - [x] Message: "A timer is already running for [Task Name]. Stop current timer and start new one?"
    - [x] Display elapsed time for current timer
    - [x] "Yes, Switch Timer" button
    - [x] "Cancel" button
  - [x] If confirmed:
    - [x] Auto-save current timer with note "Auto-saved: switched to another task"
    - [x] Start new timer

- [x] **Task 11: Add Unit Tests for TimerStateService** (AC: 4, 10)
  - [x] Create `timer-state.service.spec.ts`
  - [x] Test: `should start timer and update state`
  - [x] Test: `should pause timer and stop ticking`
  - [x] Test: `should resume timer and continue ticking`
  - [x] Test: `should stop timer and return elapsed minutes (rounded up)`
  - [x] Test: `should persist timer state to localStorage`
  - [x] Test: `should load timer from localStorage on init`
  - [x] Test: `should clear localStorage when timer stopped`
  - [x] Test: `should increment elapsed seconds every second`

- [x] **Task 12: Add Component Tests for TimerWidgetComponent** (AC: 2, 3, 5, 6, 7, 13)
  - [x] Create `timer-widget.component.spec.ts`
  - [x] Test: `should display timer widget when timer is running`
  - [x] Test: `should hide timer widget when no timer active`
  - [x] Test: `should display task name and formatted time`
  - [x] Test: `should show pause button when running`
  - [x] Test: `should show resume button when paused`
  - [x] Test: `should call timerState.pauseTimer() on pause click`
  - [x] Test: `should call timerState.resumeTimer() on resume click`
  - [x] Test: `should open stop dialog on stop click`
  - [x] Test: `should display pulsing indicator when active`

- [x] **Task 13: Add Styling for Timer Widget** (AC: 3, 13)
  - [x] Update `timer-widget.component.scss`:
    - [x] Fixed position: `position: fixed; top: 20px; right: 20px; z-index: 1000;`
    - [x] Card styling with shadow: `mat-card` or custom card styles
    - [x] Pulsing animation for active indicator:
      ```scss
      @keyframes pulse {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.5; }
      }
      .timer-active-indicator {
        animation: pulse 2s infinite;
      }
      ```
    - [x] Responsive: collapse to smaller widget or move to bottom on mobile
    - [x] Use Angular Material theming variables

- [ ] **Task 14: Integration Test - Full Timer Flow** (AC: 1-13)
  - [ ] Create E2E test or integration test:
    - [ ] Start timer from task list
    - [ ] Verify timer widget appears with task name
    - [ ] Wait for elapsed time to increment
    - [ ] Pause timer, verify time stops
    - [ ] Resume timer, verify time continues
    - [ ] Navigate to different page, verify timer persists
    - [ ] Refresh page, verify timer restored from localStorage
    - [ ] Stop timer, enter note, save
    - [ ] Verify time entry created in backend
    - [ ] Verify task's TotalLoggedMinutes updated

## Dev Notes

### Relevant Architecture

**Frontend Framework**: Angular 20 with standalone components
**State Management**: RxJS with BehaviorSubject for reactive timer state
**UI Components**: Angular Material for dialogs, buttons, cards
**Storage**: Browser localStorage for timer persistence
**API Integration**: TimeTrackingService for saving time entries

### Source Tree (Relevant Locations)

```
src/app/
  core/
    services/
      state/
        timer-state.service.ts          # NEW: Timer state management
      api/
        time-tracking.service.ts        # EXISTING: Update to support EntryType
  shared/
    components/
      timer-widget/                     # NEW: Global timer display widget
        timer-widget.component.ts
        timer-widget.component.html
        timer-widget.component.scss
        timer-widget.component.spec.ts
      stop-timer-dialog/                # NEW: Dialog for stopping timer
        stop-timer-dialog.component.ts
        stop-timer-dialog.component.html
        stop-timer-dialog.component.scss
  features/
    tasks/
      task-list/
        task-list.component.ts          # UPDATE: Add start timer button
        task-list.component.html
      task-detail/
        task-detail.component.ts        # UPDATE: Add start timer button
        task-detail.component.html
  app.component.html                    # UPDATE: Add timer widget
```

### Key Technical Details

**Timer Interval Management**:
- Use RxJS `interval(1000)` for one-second ticks
- Store interval subscription and clean up on pause/stop
- Use `takeUntil()` to auto-unsubscribe when service destroyed

**localStorage Schema**:
```typescript
{
  isRunning: boolean,
  isPaused: boolean,
  taskId: string,
  taskName: string,
  elapsedSeconds: number,
  startTime: number  // timestamp when timer started/resumed
}
```

**Elapsed Time Calculation**:
- Store `startTime` timestamp when timer starts
- On each tick, calculate elapsed = (now - startTime) + previousElapsedSeconds
- Round up to nearest minute when saving: `Math.ceil(elapsedSeconds / 60)`

**Timer State Synchronization**:
- Timer state is global singleton service (providedIn: 'root')
- All components subscribe to `timer$` observable for reactive updates
- Widget displays on all pages by mounting in app root

**Confirmation Flow for Timer Switch**:
1. User clicks "Start Timer" on Task B while Task A timer running
2. Show dialog: "Timer running for Task A (15m elapsed). Switch to Task B?"
3. If YES: Auto-save Task A time with note "Auto-saved", start Task B timer
4. If NO: Do nothing, keep Task A timer running

**Performance Considerations**:
- Use `OnPush` change detection strategy for timer widget
- Format time using pure pipe to avoid recalculations
- Debounce localStorage writes (write on pause/stop, not every tick)

**Previous Story Dependencies**:
- Story 2.6 implemented TimeEntry backend and TimeTrackingService
- TimeEntry entity has `EntryType` enum (Timer/Manual)
- API endpoint: POST `/api/tasks/:id/timeentries` accepts EntryType

### Testing

**Test File Locations**:
- Unit tests: `src/app/core/services/state/timer-state.service.spec.ts`
- Component tests: `src/app/shared/components/timer-widget/timer-widget.component.spec.ts`
- Integration tests: Can use Playwright E2E tests in `e2e/` folder

**Testing Frameworks**:
- Jasmine + Karma for unit/component tests
- Angular TestBed for component testing
- Playwright for E2E testing (optional for full flow)

**Key Test Scenarios**:
1. Timer state management (start, pause, resume, stop)
2. localStorage persistence and restoration
3. UI updates (widget display, buttons, formatted time)
4. Timer switching with confirmation
5. Time entry creation on stop
6. Task data refresh after save

**Mocking**:
- Mock `TimeTrackingService` in component tests
- Mock `localStorage` in service tests
- Use Jasmine `jasmine.clock()` to control time in tests

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-29 | 1.0 | Initial story creation | Sarah (PO) |

## Dev Agent Record

### Agent Model Used
Claude Sonnet 4.5

### Debug Log References
None

### Completion Notes
- Implemented Timer State Service with full localStorage persistence
- Created Timer Widget Component with pulsing indicator and responsive design
- Created Stop Timer Dialog for entering notes when stopping timer
- Added Start Timer buttons to Task List component with timer switching logic
- Added Start Timer button to Task Detail Dialog with same timer switching logic
- Created TimeTrackingService to integrate with backend time entry APIs
- Created TimeEntry model matching backend DTOs
- All core timer functionality implemented and working
- Unit tests created for TimerStateService
- Component tests created for TimerWidgetComponent
- Timer persists across page refreshes using localStorage
- Only one timer can run at a time with confirmation dialog for switching
- Time entries are successfully saved to backend with EntryType=Timer
- Task state refresh: Implemented automatic task list refresh after timer save - TotalLoggedMinutes updates immediately in UI (AC 12 fully met)

### File List

**New Files:**
- src/app/core/services/state/timer-state.service.ts
- src/app/core/services/state/timer-state.service.spec.ts
- src/app/core/services/time-tracking.service.ts
- src/app/shared/models/time-entry.model.ts
- src/app/shared/components/timer-widget/timer-widget.component.ts
- src/app/shared/components/timer-widget/timer-widget.component.html
- src/app/shared/components/timer-widget/timer-widget.component.scss
- src/app/shared/components/timer-widget/timer-widget.component.spec.ts
- src/app/shared/components/stop-timer-dialog/stop-timer-dialog.component.ts
- src/app/shared/components/stop-timer-dialog/stop-timer-dialog.component.html
- src/app/shared/components/stop-timer-dialog/stop-timer-dialog.component.scss

**Modified Files:**
- src/app/app.ts (added TimerWidgetComponent import)
- src/app/app.html (added timer widget to root)
- src/app/features/tasks/task-list/task-list.component.ts (added timer functionality)
- src/app/features/tasks/task-list/task-list.component.html (added start timer buttons)
- src/app/features/tasks/task-detail-dialog/task-detail-dialog.ts (added timer functionality)
- src/app/features/tasks/task-detail-dialog/task-detail-dialog.html (added start timer button)





