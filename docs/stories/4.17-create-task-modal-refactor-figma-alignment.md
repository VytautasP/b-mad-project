# Story 4.17: Create Task Modal - Refactor & Figma Alignment

## Status

Ready for Review

## Story

**As a** TaskFlow user,
**I want** the Create Task modal to match the approved Figma design,
**so that** creating a task feels polished, predictable, and consistent across the shellbar, task list, and project entry points.

## Design Reference

- **Figma file:** TaskFlow - Create Task Modal via figma_local MCP server
- **Node:** `170:144` (`Modal Content`)
- **Supporting nodes:** `170:145` (Header), `170:154` (Body/Form), `170:226` (Footer)
- **Primary UX reference:** `docs/front-end-spec.md`
- **Triggered from:** task list create CTA, shellbar Create Task button, project-level create action

## Figma Design Summary

The approved design defines a centered dialog with a clear three-part structure: **header**, **form body**, and **footer actions**. The current Angular `TaskFormComponent` already supports the necessary fields and submission flow, but the modal presentation is still close to default Angular Material styling. This story refactors the dialog to match the approved Figma layout while preserving the existing create-task behavior.

### Modal Container (`170:144`)
- Width: `672px`
- Height: auto / hug content (`631px` in the source frame)
- Background: `#FFFFFF`
- Border: `1px solid #E2E8F0`
- Border radius: `8px`
- Shadow: `0 25px 50px -12px rgba(0, 0, 0, 0.25)`
- Layout: vertical, clipped content, no internal outer padding on the shell itself

### Header (`170:145`)
- Height: `77px`
- Padding: `20px` vertical, `32px` horizontal
- Bottom border: `1px solid #E2E8F0`
- Title: `Create New Task`
- Title typography: Manrope 700 / `18px` / `28px` / `-0.45px` letter-spacing / `#111418`
- Right-aligned close affordance above the form body

### Body/Form (`170:154`)
- Padding: `24px` top, `32px` left/right, `40px` bottom
- Vertical gap between groups: `24px`
- Field order:
  1. Task Name (full width)
  2. Description (full width textarea)
  3. Three-column planning row: Due Date, Priority, Status
  4. Type (full width)
- Labels: Manrope 600 / `14px`
- Desktop planning row: three equal-width columns with `24px` gap
- Tablet/mobile: planning row stacks vertically in the same field order

### Footer (`170:226`)
- Height: `81px`
- Padding: `20px` vertical, `32px` horizontal
- Background: `#F6F7F8`
- Top border: `1px solid #E2E8F0`
- Right-aligned action group with `12px` gap
- Secondary button: `Cancel`
  - Text color: `#64748B`
  - Border radius: `8px`
  - Padding: `20px` horizontal / `10px` vertical
- Primary button: `Create Task`
  - Background: `#137FEC`
  - Text color: `#FFFFFF`
  - Border radius: `8px`
  - Padding: `32px` horizontal / `10px` vertical
  - Shadow: layered blue button shadow per the Figma source

## Acceptance Criteria

1. **All create entry points** open the same refactored Create Task dialog from `TaskFormComponent`: task list, shellbar, and project view.
2. **Dialog container** matches the Figma shell: centered, `672px` desktop width, white background, `8px` radius, `1px` `#E2E8F0` border, and `0 25px 50px -12px rgba(0, 0, 0, 0.25)` shadow.
3. **Dialog header** is implemented as a distinct section with `32px` horizontal / `20px` vertical padding, bottom divider, title "Create New Task", and a close button aligned right.
4. **Dialog body** follows the Figma field order exactly: Task Name, Description, Due Date / Priority / Status row, then Type.
5. **Task Name** remains required and is visually emphasized as the first field in the modal.
6. **Description** renders as a full-width textarea directly beneath Task Name with the updated spacing and label treatment.
7. **Due Date, Priority, and Status** render in a three-column row at desktop widths and stack vertically on narrower screens without changing field order.
8. **Type** renders as a full-width field beneath the planning row.
9. **Footer section** is visually separate from the form body with `#F6F7F8` background, top divider, right-aligned actions, and visible `Cancel` plus `Create Task` buttons.
10. **Primary action styling** matches the approved Figma treatment: brand-blue fill, white text, `8px` radius, stable width during loading, and stronger visual emphasis than Cancel.
11. **Cancel and close button behavior** both dismiss the dialog and return focus to the invoking control when the form is not actively submitting.
12. **Submit success behavior** remains unchanged: the dialog closes, the originating surface refreshes, and success feedback appears.
13. **Submit failure behavior** remains in-modal: entered values are preserved, an inline error message appears within the form, and the user can retry without re-entering the task.
14. **Accessibility requirements** are met for the modal: dialog name, visible labels, keyboard traversal in visual order, Escape behavior, focus return, icon button accessible names, and visible focus states.
15. **Typography and tokens** match the approved spec for this modal: Manrope inside the dialog, `#111418` primary text, `#64748B` secondary text, `#137FEC` primary CTA, `#E2E8F0` structural borders.
16. **Responsive behavior** keeps the modal usable from `320px` upward, using viewport-fit width with safe margins and stacked planning controls on smaller breakpoints.
17. **Shared-component regressions are avoided:** edit mode and embedded usage of `TaskFormComponent` continue to function, with correct labels and submission behavior.
18. **Frontend tests** are updated to cover the refactored create dialog layout, close/cancel behavior, loading/error states, and responsive field grouping.

## Tasks / Subtasks

- [x] **Task 1: Audit current create-task dialog flow and shared component usage** (AC: 1, 17)
  - [x] Confirm all create entry points that open `TaskFormComponent`: task list, shellbar, and project view.
  - [x] Confirm shared `TaskFormComponent` usage in create, edit, and embedded contexts.
  - [x] Document any current behavior that must be preserved during the refactor: validation, success close, snackbar refresh, and Escape handling.

- [x] **Task 2: Refactor `TaskFormComponent` template into header/body/footer sections** (AC: 3, 4, 5, 6, 7, 8, 9)
  - [x] Replace the current single stacked form layout with explicit header, body, and footer wrappers.
  - [x] Add a dedicated header title region and close button for dialog mode.
  - [x] Move `Cancel` into the dialog footer so dialog mode has both secondary and primary actions.
  - [x] Group Due Date, Priority, and Status into one responsive row.
  - [x] Keep Type as a separate full-width field beneath the grouped row.

- [x] **Task 3: Apply Figma-aligned modal styling in `task-form.component.scss`** (AC: 2, 3, 6, 7, 9, 10, 15, 16)
  - [x] Implement the `672px` modal shell styling: border, radius, shadow, clipping, spacing.
  - [x] Style header divider, body padding, footer background, and footer divider.
  - [x] Apply Manrope typography, primary/secondary text colors, and updated label hierarchy.
  - [x] Style the three-column planning row and its stacked mobile variant.
  - [x] Ensure loading/error states do not cause footer reflow or button-width jitter.

- [x] **Task 4: Update `TaskFormComponent` interaction logic for the refactored modal shell** (AC: 11, 12, 13, 14, 17)
  - [x] Add a dedicated close-button handler for dialog mode.
  - [x] Prevent dismissal actions while a blocking submit is in progress if needed to protect consistency.
  - [x] Preserve entered values on failed create requests.
  - [x] Keep existing success behavior intact for create mode.
  - [x] Verify edit mode and embedded mode still render correct labels and action text.

- [x] **Task 5: Standardize dialog opener configuration across Angular entry points** (AC: 1, 2, 16)
  - [x] Update `task-list.component.ts` create dialog opener to use the new modal width and shared panel/backdrop styling.
  - [x] Update `shellbar.ts` create dialog opener to use the same dialog configuration.
  - [x] Update `project.ts` create dialog opener to use the same dialog configuration.
  - [x] Reuse existing dialog motion timing where available so the create modal feels consistent with the rest of the app.

- [x] **Task 6: Add shared dialog shell styles in global styles where required** (AC: 2, 10, 15)
  - [x] Add a dedicated dialog `panelClass` / backdrop styling for the Create Task modal if component-scoped styles are insufficient.
  - [x] Ensure the dialog surface padding is fully controlled by the component layout instead of Angular Material defaults.
  - [x] Reuse existing motion tokens and focus-visible rules already defined in `src/styles.css`.

- [x] **Task 7: Implement accessibility and responsive behavior** (AC: 7, 11, 14, 16)
  - [x] Ensure the dialog exposes a clear accessible name and the close button has an accessible label.
  - [x] Verify focus order follows the visual layout.
  - [x] Ensure Escape closes the dialog and returns focus to the opener when not submitting.
  - [x] Verify all touch targets meet minimum sizing on smaller screens.
  - [x] Stack the planning row vertically below tablet/mobile breakpoints without altering field order.

## Dev Notes

- **Priority:** P1
- **Epic reference:** `docs/prd/4.0-ux-friendly-ui-refinement-prioritized.md`
- **Primary UX spec:** `docs/front-end-spec.md`
- **Related stories:** `docs/stories/1.5-task-management-frontend-create-list.md`, `docs/stories/4.4-accessibility-baseline-wcag-2.2-aa.md`, `docs/stories/4.6-responsive-layout-adaptation.md`, `docs/stories/4.7-motion-microinteraction-consistency.md`, `docs/stories/4.12-shellbar-header-refactor-figma-alignment.md`

### Current State

- `TaskFormComponent` currently handles both create and edit mode in a single standalone Angular component.
- The current create dialog uses default Angular Material form-field presentation with a `600px` dialog width and no dedicated modal shell matching the approved Figma structure.
- The current dialog layout stacks all fields vertically and only shows `Cancel` in embedded mode, not in the main create dialog flow.
- Escape close behavior is implemented directly on the component and should be preserved, but brought into alignment with the refactored dialog states.
- Create dialog openers currently exist in:
  - `src/app/features/tasks/task-list/task-list.component.ts`
  - `src/app/shared/components/shellbar/shellbar.ts`
  - `src/app/features/project/project.ts`

### Angular Implementation Constraints

- Keep the refactor frontend-only. No API contract or DTO changes are required.
- Maintain Angular standalone component structure and the current reactive form approach.
- Continue routing all persistence through `TaskService`; do not introduce direct HTTP calls from the component.
- Keep the component focused and avoid expanding this story into edit-flow redesign or new task fields.

### Design Tokens Summary

| Token | Value | Usage |
|---|---|---|
| Modal width | `672px` | Desktop dialog width |
| Modal background | `#FFFFFF` | Dialog surface |
| Modal border | `1px solid #E2E8F0` | Shell border and dividers |
| Modal radius | `8px` | Shell and buttons |
| Modal shadow | `0 25px 50px -12px rgba(0, 0, 0, 0.25)` | Dialog elevation |
| Header/footer horizontal padding | `32px` | Shell sections |
| Header/footer vertical padding | `20px` | Shell sections |
| Body top padding | `24px` | Form body |
| Body bottom padding | `40px` | Form body |
| Group spacing | `24px` | Vertical rhythm and planning-row gap |
| Primary text | `#111418` | Title and key text |
| Secondary text | `#64748B` | Secondary text and quiet controls |
| Border / divider | `#E2E8F0` | Structural separators |
| Footer background | `#F6F7F8` | Action bar surface |
| Brand blue | `#137FEC` | Primary action |
| Title font | Manrope 700 / `18px` / `28px` | Header title |
| Label font | Manrope 600 / `14px` | Field labels |
| Body font | Manrope 400 / `14px` / `20px` | Field text |

## File List

| File | Status | Description |
|------|--------|-------------|
| `src/app/shared/utils/task-form-dialog.utils.ts` | Create | Shared create-task dialog config, dialog data types, and dialog shell classes |
| `src/app/features/tasks/services/task.service.ts` | Modify | Added task refresh request stream for cross-surface create success refreshes |
| `src/app/features/tasks/task-form/task-form.component.ts` | Modify | Dialog data support, focus management, dismiss guards, and preserved create/edit flows |
| `src/app/features/tasks/task-form/task-form.component.html` | Modify | Figma-aligned header/body/footer shell, field order, and footer actions |
| `src/app/features/tasks/task-form/task-form.component.scss` | Modify | Modal shell styling, planning grid, responsive layout, and action button treatment |
| `src/app/features/tasks/task-form/task-form.component.spec.ts` | Modify | Coverage for dialog shell, dismiss behavior, error retention, and edit mode |
| `src/app/features/tasks/task-list/task-list.component.ts` | Modify | Shared create dialog config and external task refresh handling |
| `src/app/features/tasks/task-list/task-list.component.spec.ts` | Modify | Coverage for shared dialog config and external refresh requests |
| `src/app/shared/components/shellbar/shellbar.ts` | Modify | Shared create dialog config, refresh requests, and create success feedback |
| `src/app/shared/components/shellbar/shellbar.spec.ts` | Modify | Coverage for shared dialog config and shellbar-triggered refresh behavior |
| `src/app/features/project/project.ts` | Modify | Shared create dialog config and project-surface refresh handling |
| `src/app/features/project/project.spec.ts` | Modify | Coverage for shared dialog config and task refresh requests |
| `src/app/features/tasks/timeline/timeline.ts` | Modify | Timeline refresh handling for shellbar-triggered task creation |
| `src/app/features/tasks/timeline/timeline.spec.ts` | Modify | Coverage for timeline refresh requests |
| `src/styles.css` | Modify | Global create-task dialog panel and backdrop overrides for Angular Material |

## Dev Agent Record

### Agent Model Used
GPT-5.4

### Completion Notes List
- Refactored `TaskFormComponent` to match Figma node `170:144` with explicit header, body, and footer sections while preserving create, edit, and embedded behavior.
- Standardized create-task dialog opening through a shared `getCreateTaskDialogConfig()` helper so task list, shellbar, and project entry points use the same sizing, motion, shell, and focus behavior.
- Added `TaskService.taskRefreshRequests$` so shellbar-triggered creates refresh task list, project, and timeline surfaces without coupling refresh logic to `tasks$` state updates.
- Updated targeted task-related specs and validated the refactor with targeted Angular tests plus a full frontend regression run (`26` test files, `387` tests passed).
- Verified local compilation with `npx ng build --configuration development`; the default production build remains environment-blocked by Google Fonts inlining network access, not by application code.
- No new dependencies, API contracts, DTOs, or environment variables were introduced.

### Debug Log References
- `npx ng test --watch=false --reporters=verbose --include src/app/features/tasks/task-form/task-form.component.spec.ts --include src/app/features/tasks/task-list/task-list.component.spec.ts --include src/app/shared/components/shellbar/shellbar.spec.ts --include src/app/features/project/project.spec.ts --include src/app/features/tasks/timeline/timeline.spec.ts`
- `npx ng test --watch=false`
- `npx ng build --configuration development`
- `npm run build` fails in this environment because Angular cannot inline Google Fonts over the network during optimization.

## Change Log

| Date | Change | Author |
|------|--------|--------|
| 2026-03-29 | Created implementation-ready Angular frontend story for refactoring the Create Task modal to match approved Figma node 170:144. | Sally (UX Expert) |
| 2026-03-29 | Implemented the Figma-aligned create-task modal refactor, standardized dialog openers, added shared refresh handling, and validated with targeted plus full frontend test runs. | James (Dev Agent) |