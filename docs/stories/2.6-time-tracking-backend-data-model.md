# Story 2.6: Time Tracking Backend and Data Model

## Status

Approved

## Story

**As a** freelancer,
**I want** the backend to store time entries for tasks with user attribution,
**so that** my billable hours are accurately tracked and attributed.

## Acceptance Criteria

1. TimeEntry entity created with fields: Id (GUID), TaskId (FK), UserId (FK), Minutes (int), EntryDate (datetime), Note (nullable, max 500 chars), EntryType (enum: Timer/Manual)
2. Database migration created for TimeEntry table with indexes on TaskId and UserId
3. TimeEntryService created with methods: LogTime(taskId, userId, minutes, note, entryType), GetTaskTimeEntries(taskId), DeleteTimeEntry(entryId)
4. API endpoint created: POST /api/tasks/:id/timeentries to log time
5. API endpoint created: GET /api/tasks/:id/timeentries returns all time entries for task
6. API endpoint created: DELETE /api/timeentries/:id to delete specific entry
7. Task entity updated with computed LoggedMinutes field (sum of associated TimeEntry minutes)
8. Task list endpoint includes TotalLoggedMinutes calculated from TimeEntry sum
9. Authorization enforced—users can only create time entries for themselves, only entry creator can delete
10. Validation enforces positive minutes (> 0) and reasonable maximum (< 24 hours per entry)
11. Time entries include user name for display in time log
12. Unit tests cover time logging logic and authorization
13. Integration tests validate time entry endpoints with multiple entries per task

## Tasks / Subtasks

- [ ] **Task 1: Create TimeEntry Entity in Abstractions Layer** (AC: 1)
  - [ ] Create `TimeEntry.cs` in `TaskFlow.Abstractions/Entities/`:
    - [ ] Property: `Id` (Guid, primary key)
    - [ ] Property: `TaskId` (Guid, foreign key to Task.Id, required)
    - [ ] Property: `UserId` (Guid, foreign key to User.Id, required)
    - [ ] Property: `Minutes` (int, required, must be > 0)
    - [ ] Property: `EntryDate` (DateTime, required, defaults to UTC now)
    - [ ] Property: `Note` (string, nullable, max 500 chars)
    - [ ] Property: `EntryType` (EntryType enum, required)
    - [ ] Navigation Property: `Task` (Task entity)
    - [ ] Navigation Property: `User` (User entity)
  - [ ] Create `EntryType` enum in `TaskFlow.Abstractions/Constants/`:
    - [ ] Values: `Timer = 0`, `Manual = 1`
  - [ ] Add XML documentation comments for all properties

- [ ] **Task 2: Create TimeEntry DTOs in Abstractions Layer** (AC: 4, 5, 11)
  - [ ] Create `TimeEntryCreateDto.cs` in `TaskFlow.Abstractions/DTOs/TimeEntries/`:
    - [ ] Property: `Minutes` (int, required, Range(1, 1440) for max 24 hours)
    - [ ] Property: `Note` (string, optional, MaxLength(500))
    - [ ] Property: `EntryDate` (DateTime, optional, defaults to current date)
    - [ ] Property: `EntryType` (EntryType enum, required)
    - [ ] Add DataAnnotations for validation
  - [ ] Create `TimeEntryResponseDto.cs` in `TaskFlow.Abstractions/DTOs/TimeEntries/`:
    - [ ] Property: `Id` (Guid)
    - [ ] Property: `TaskId` (Guid)
    - [ ] Property: `UserId` (Guid)
    - [ ] Property: `UserName` (string) - for display
    - [ ] Property: `Minutes` (int)
    - [ ] Property: `EntryDate` (DateTime)
    - [ ] Property: `Note` (string, nullable)
    - [ ] Property: `EntryType` (string - "Timer" or "Manual")
    - [ ] Property: `CreatedAt` (DateTime)

- [ ] **Task 3: Update Task Entity with Time Tracking Support** (AC: 7)
  - [ ] Update `Task.cs` in `TaskFlow.Abstractions/Entities/`:
    - [ ] Add Navigation Property: `ICollection<TimeEntry> TimeEntries { get; set; }`
    - [ ] Add computed property (not mapped): `int TotalLoggedMinutes => TimeEntries?.Sum(te => te.Minutes) ?? 0`
    - [ ] Initialize TimeEntries collection in constructor: `TimeEntries = new List<TimeEntry>()`
  - [ ] Update `TaskResponseDto.cs` in `TaskFlow.Abstractions/DTOs/Tasks/`:
    - [ ] Add Property: `TotalLoggedMinutes` (int, default 0)
    - [ ] This will be calculated during mapping from Task.TimeEntries

- [ ] **Task 4: Create EF Core Configuration for TimeEntry** (AC: 2)
  - [ ] Create `TimeEntryConfiguration.cs` in `TaskFlow.Infrastructure/Configurations/`:
    - [ ] Implement `IEntityTypeConfiguration<TimeEntry>`
    - [ ] Configure table name: `time_entries`
    - [ ] Configure primary key: `Id`
    - [ ] Configure foreign key: `TaskId` with cascade delete (when task deleted, time entries deleted)
    - [ ] Configure foreign key: `UserId` with restrict delete (preserve time entries if user deleted)
    - [ ] Configure index on `TaskId` for fast lookups
    - [ ] Configure index on `UserId` for user-specific queries
    - [ ] Configure index on `EntryDate` for time-range queries
    - [ ] Configure `Note` as nullable with max length 500
    - [ ] Configure `EntryType` as integer (enum stored as int)
    - [ ] Configure `Minutes` with check constraint: `CHECK (minutes > 0 AND minutes <= 1440)`
  - [ ] Update `ApplicationDbContext.cs` in `TaskFlow.Infrastructure/Data/`:
    - [ ] Add `DbSet<TimeEntry> TimeEntries { get; set; }`
    - [ ] Apply `TimeEntryConfiguration` in `OnModelCreating`

- [ ] **Task 5: Create and Apply EF Core Migration** (AC: 2)
  - [ ] Run command: `dotnet ef migrations add AddTimeEntryEntity --project TaskFlow.Infrastructure --startup-project TaskFlow.Api`
  - [ ] Review generated migration file:
    - [ ] Verify table name is `time_entries`
    - [ ] Verify indexes created on TaskId, UserId, EntryDate
    - [ ] Verify foreign key constraints are correct
    - [ ] Verify check constraint for Minutes column
  - [ ] Run command: `dotnet ef database update --project TaskFlow.Infrastructure --startup-project TaskFlow.Api`
  - [ ] Verify migration applied successfully in Supabase PostgreSQL

- [ ] **Task 6: Create ITimeEntryRepository Interface** (AC: 3, 5, 6)
  - [ ] Create `ITimeEntryRepository.cs` in `TaskFlow.Abstractions/Interfaces/Repositories/`:
    - [ ] Method: `Task<TimeEntry> CreateAsync(TimeEntry timeEntry, CancellationToken cancellationToken)`
    - [ ] Method: `Task<List<TimeEntry>> GetByTaskIdAsync(Guid taskId, CancellationToken cancellationToken)`
    - [ ] Method: `Task<TimeEntry?> GetByIdAsync(Guid id, CancellationToken cancellationToken)`
    - [ ] Method: `Task DeleteAsync(TimeEntry timeEntry, CancellationToken cancellationToken)`
    - [ ] Method: `Task<int> GetTotalMinutesByTaskIdAsync(Guid taskId, CancellationToken cancellationToken)`
  - [ ] Update `IUnitOfWork.cs` to include:
    - [ ] Property: `ITimeEntryRepository TimeEntries { get; }`

- [ ] **Task 7: Implement TimeEntryRepository** (AC: 3, 5, 6)
  - [ ] Create `TimeEntryRepository.cs` in `TaskFlow.Infrastructure/Repositories/`:
    - [ ] Inject `ApplicationDbContext`
    - [ ] Implement `ITimeEntryRepository`
    - [ ] Method: `CreateAsync`:
      - [ ] Add timeEntry to context
      - [ ] Return the created entity
    - [ ] Method: `GetByTaskIdAsync`:
      - [ ] Query: `_context.TimeEntries.Where(te => te.TaskId == taskId).Include(te => te.User).AsNoTracking().OrderByDescending(te => te.EntryDate).ToListAsync()`
      - [ ] Include User navigation for UserName display
    - [ ] Method: `GetByIdAsync`:
      - [ ] Query: `_context.TimeEntries.Include(te => te.User).FirstOrDefaultAsync(te => te.Id == id)`
    - [ ] Method: `DeleteAsync`:
      - [ ] Remove timeEntry from context
    - [ ] Method: `GetTotalMinutesByTaskIdAsync`:
      - [ ] Query: `_context.TimeEntries.Where(te => te.TaskId == taskId).SumAsync(te => te.Minutes)`
  - [ ] Update `UnitOfWork.cs` to initialize TimeEntries repository

- [ ] **Task 8: Create ITimeEntryService Interface** (AC: 3, 9, 10)
  - [ ] Create `ITimeEntryService.cs` in `TaskFlow.Abstractions/Interfaces/Services/`:
    - [ ] Method: `Task<TimeEntryResponseDto> LogTimeAsync(Guid taskId, Guid userId, TimeEntryCreateDto dto, CancellationToken cancellationToken)`
    - [ ] Method: `Task<List<TimeEntryResponseDto>> GetTaskTimeEntriesAsync(Guid taskId, Guid requestingUserId, CancellationToken cancellationToken)`
    - [ ] Method: `Task DeleteTimeEntryAsync(Guid entryId, Guid requestingUserId, CancellationToken cancellationToken)`

- [ ] **Task 9: Implement TimeEntryService with Business Logic** (AC: 3, 9, 10)
  - [ ] Create `TimeEntryService.cs` in `TaskFlow.Core/Services/`:
    - [ ] Inject: `IUnitOfWork`
    - [ ] Inject: `ILogger<TimeEntryService>`
    - [ ] Implement `ITimeEntryService`
    - [ ] Method: `LogTimeAsync`:
      - [ ] Validate taskId exists (call TaskRepository.GetByIdAsync)
      - [ ] Throw NotFoundException if task not found
      - [ ] Validate userId matches requestingUserId (AC: 9 - users can only log time for themselves)
      - [ ] Validate Minutes > 0 and <= 1440 (AC: 10)
      - [ ] Create TimeEntry entity from DTO
      - [ ] Set UserId, TaskId, CreatedAt fields
      - [ ] Call TimeEntryRepository.CreateAsync
      - [ ] Call UnitOfWork.SaveChangesAsync
      - [ ] Map to TimeEntryResponseDto (include UserName from User entity)
      - [ ] Log structured log: "User {UserId} logged {Minutes} minutes for task {TaskId}"
      - [ ] Return TimeEntryResponseDto
    - [ ] Method: `GetTaskTimeEntriesAsync`:
      - [ ] Validate task exists and user has access (owns task or is assigned)
      - [ ] Call TimeEntryRepository.GetByTaskIdAsync
      - [ ] Map list to TimeEntryResponseDto (include UserName)
      - [ ] Return list ordered by EntryDate descending
    - [ ] Method: `DeleteTimeEntryAsync`:
      - [ ] Fetch TimeEntry by id
      - [ ] Throw NotFoundException if not found
      - [ ] Validate requestingUserId == timeEntry.UserId (AC: 9 - only creator can delete)
      - [ ] Throw UnauthorizedException if not authorized
      - [ ] Call TimeEntryRepository.DeleteAsync
      - [ ] Call UnitOfWork.SaveChangesAsync
      - [ ] Log structured log: "User {UserId} deleted time entry {EntryId}"

- [ ] **Task 10: Create TimeTrackingController API Endpoints** (AC: 4, 5, 6)
  - [ ] Update or create `TimeTrackingController.cs` in `TaskFlow.Api/Controllers/`:
    - [ ] Add `[ApiController]`, `[Route("api/tasks")]`, `[Authorize]` attributes
    - [ ] Inject: `ITimeEntryService`
    - [ ] Inject: `ILogger<TimeTrackingController>`
    - [ ] Helper method: `GetUserId()` to extract user ID from claims
    - [ ] Endpoint: `[HttpPost("{taskId:guid}/timeentries")]` LogTime (AC: 4):
      - [ ] Parameter: `Guid taskId` from route
      - [ ] Parameter: `TimeEntryCreateDto dto` from body
      - [ ] Extract userId from JWT claims
      - [ ] Call `TimeEntryService.LogTimeAsync(taskId, userId, dto, cancellationToken)`
      - [ ] Return 201 Created with TimeEntryResponseDto
      - [ ] Add ProducesResponseType attributes (201, 400, 401, 404)
    - [ ] Endpoint: `[HttpGet("{taskId:guid}/timeentries")]` GetTimeEntries (AC: 5):
      - [ ] Parameter: `Guid taskId` from route
      - [ ] Extract userId from JWT claims
      - [ ] Call `TimeEntryService.GetTaskTimeEntriesAsync(taskId, userId, cancellationToken)`
      - [ ] Return 200 OK with List<TimeEntryResponseDto>
      - [ ] Add ProducesResponseType attributes (200, 401, 404)
  - [ ] Create separate endpoint group `[Route("api/timeentries")]`:
    - [ ] Endpoint: `[HttpDelete("{id:guid}")]` DeleteTimeEntry (AC: 6):
      - [ ] Parameter: `Guid id` from route
      - [ ] Extract userId from JWT claims
      - [ ] Call `TimeEntryService.DeleteTimeEntryAsync(id, userId, cancellationToken)`
      - [ ] Return 204 No Content
      - [ ] Add ProducesResponseType attributes (204, 401, 403, 404)

- [ ] **Task 11: Update TaskService to Include TotalLoggedMinutes** (AC: 8)
  - [ ] Update `TaskService.cs` in `TaskFlow.Core/Services/`:
    - [ ] In `GetTasksAsync` method:
      - [ ] Include TimeEntries in query: `.Include(t => t.TimeEntries)`
      - [ ] Calculate TotalLoggedMinutes during mapping to TaskResponseDto
      - [ ] Use `task.TimeEntries.Sum(te => te.Minutes)` or 0 if null
    - [ ] In `GetTaskByIdAsync` method:
      - [ ] Include TimeEntries in query: `.Include(t => t.TimeEntries)`
      - [ ] Calculate TotalLoggedMinutes during mapping
  - [ ] Update TaskResponseDto mapping logic to populate TotalLoggedMinutes

- [ ] **Task 12: Create Unit Tests for TimeEntryService** (AC: 12)
  - [ ] Create `TimeEntryServiceTests.cs` in `TaskFlow.Tests/Unit/Services/`:
    - [ ] Test: `LogTimeAsync_ShouldCreateTimeEntry_WhenValidInput`:
      - [ ] Arrange: Mock UnitOfWork, valid DTO, existing task
      - [ ] Act: Call LogTimeAsync
      - [ ] Assert: TimeEntry created, SaveChanges called, returns TimeEntryResponseDto
    - [ ] Test: `LogTimeAsync_ShouldThrowNotFoundException_WhenTaskNotFound`:
      - [ ] Arrange: Mock returns null task
      - [ ] Act & Assert: Throws NotFoundException
    - [ ] Test: `LogTimeAsync_ShouldThrowValidationException_WhenMinutesIsZero`:
      - [ ] Arrange: DTO with Minutes = 0
      - [ ] Act & Assert: Throws ValidationException
    - [ ] Test: `LogTimeAsync_ShouldThrowValidationException_WhenMinutesExceeds24Hours`:
      - [ ] Arrange: DTO with Minutes = 1500 (> 1440)
      - [ ] Act & Assert: Throws ValidationException
    - [ ] Test: `DeleteTimeEntryAsync_ShouldDeleteEntry_WhenUserIsCreator`:
      - [ ] Arrange: Mock TimeEntry where UserId matches requestingUserId
      - [ ] Act: Call DeleteTimeEntryAsync
      - [ ] Assert: Delete called, SaveChanges called
    - [ ] Test: `DeleteTimeEntryAsync_ShouldThrowUnauthorizedException_WhenUserIsNotCreator` (AC: 9):
      - [ ] Arrange: Mock TimeEntry where UserId != requestingUserId
      - [ ] Act & Assert: Throws UnauthorizedException
    - [ ] Test: `GetTaskTimeEntriesAsync_ShouldReturnOrderedList_WhenMultipleEntries`:
      - [ ] Arrange: Mock returns 3 entries with different dates
      - [ ] Act: Call GetTaskTimeEntriesAsync
      - [ ] Assert: Returns list ordered by EntryDate descending

- [ ] **Task 13: Create Integration Tests for TimeTracking API** (AC: 13)
  - [ ] Create `TimeTrackingControllerTests.cs` in `TaskFlow.Tests/Integration/Controllers/`:
    - [ ] Test: `PostTimeEntry_ShouldReturn201_WhenValidRequest`:
      - [ ] Arrange: Authenticated client, existing task, valid TimeEntryCreateDto
      - [ ] Act: POST /api/tasks/{taskId}/timeentries
      - [ ] Assert: 201 Created, response contains TimeEntryResponseDto with Id
    - [ ] Test: `PostTimeEntry_ShouldReturn400_WhenMinutesInvalid`:
      - [ ] Arrange: DTO with Minutes = 0
      - [ ] Act: POST /api/tasks/{taskId}/timeentries
      - [ ] Assert: 400 Bad Request
    - [ ] Test: `GetTimeEntries_ShouldReturn200WithList_WhenEntriesExist`:
      - [ ] Arrange: Create 3 time entries for task
      - [ ] Act: GET /api/tasks/{taskId}/timeentries
      - [ ] Assert: 200 OK, returns array with 3 entries
    - [ ] Test: `DeleteTimeEntry_ShouldReturn204_WhenUserIsCreator`:
      - [ ] Arrange: Create time entry as user A, authenticate as user A
      - [ ] Act: DELETE /api/timeentries/{entryId}
      - [ ] Assert: 204 No Content, entry removed from database
    - [ ] Test: `DeleteTimeEntry_ShouldReturn403_WhenUserIsNotCreator` (AC: 9):
      - [ ] Arrange: Create time entry as user A, authenticate as user B
      - [ ] Act: DELETE /api/timeentries/{entryId}
      - [ ] Assert: 403 Forbidden
    - [ ] Test: `GetTasks_ShouldIncludeTotalLoggedMinutes_WhenTimeEntriesExist` (AC: 8):
      - [ ] Arrange: Create task, add 2 time entries (60 + 120 minutes)
      - [ ] Act: GET /api/tasks
      - [ ] Assert: Task in response has TotalLoggedMinutes = 180

- [ ] **Task 14: Register Services in Dependency Injection** (AC: 3)
  - [ ] Update `ServiceCollectionExtensions.cs` in `TaskFlow.Api/Extensions/`:
    - [ ] Register `ITimeEntryService` with `TimeEntryService` as scoped
    - [ ] Verify UnitOfWork registration includes TimeEntryRepository

- [ ] **Task 15: Update Swagger Documentation** (AC: 4, 5, 6)
  - [ ] Verify swagger.json or OpenAPI spec includes new endpoints:
    - [ ] POST /api/tasks/{taskId}/timeentries
    - [ ] GET /api/tasks/{taskId}/timeentries
    - [ ] DELETE /api/timeentries/{id}
  - [ ] Add XML documentation comments to controller methods
  - [ ] Test endpoints in Swagger UI

## Dev Notes

### Architecture Context

**Project Structure (Clean Architecture):**
- **TaskFlow.Abstractions**: Shared contracts with no dependencies (entities, DTOs, interfaces)
- **TaskFlow.Core**: Business logic layer (services, validators)
- **TaskFlow.Infrastructure**: Data access layer (EF Core, repositories, DbContext)
- **TaskFlow.Api**: Presentation layer (controllers, middleware)

**Entity Layer:**
TimeEntry entity belongs in `TaskFlow.Abstractions/Entities/` as it's a core domain model. Navigation properties link to Task and User entities.

**Repository Pattern:**
All data access goes through repositories. Controllers call services, services call repositories via UnitOfWork. Use `.AsNoTracking()` for read-only queries.

**Authorization:**
Backend enforces authorization at service layer. Users can only create time entries for themselves (AC: 9), and only the entry creator can delete their own entries.

**Validation:**
- DataAnnotations on DTOs for basic validation (Range, MaxLength)
- Service layer validates business rules (task exists, user permissions)
- Database enforces check constraints (Minutes > 0 AND Minutes <= 1440)

**Time Representation:**
- Store minutes as int (simpler than TimeSpan for calculations)
- 1440 minutes = 24 hours maximum per entry
- Frontend converts to hours/minutes format for display

### Database Schema

**time_entries Table:**
```sql
CREATE TABLE time_entries (
    id UUID PRIMARY KEY,
    task_id UUID NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
    minutes INT NOT NULL CHECK (minutes > 0 AND minutes <= 1440),
    entry_date TIMESTAMP NOT NULL,
    note VARCHAR(500),
    entry_type INT NOT NULL,  -- 0=Timer, 1=Manual
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_time_entries_task_id ON time_entries(task_id);
CREATE INDEX idx_time_entries_user_id ON time_entries(user_id);
CREATE INDEX idx_time_entries_entry_date ON time_entries(entry_date);
```

**Foreign Key Behaviors:**
- TaskId: CASCADE DELETE (deleting task deletes time entries)
- UserId: RESTRICT DELETE (preserve time entries if user deleted - historical data)

### API Endpoints

**POST /api/tasks/{taskId}/timeentries** (AC: 4)
- Creates time entry for task
- Request Body: TimeEntryCreateDto (Minutes, Note, EntryDate, EntryType)
- Response: 201 Created with TimeEntryResponseDto
- Auth: Required, user can only log time for themselves

**GET /api/tasks/{taskId}/timeentries** (AC: 5)
- Retrieves all time entries for task
- Response: 200 OK with List<TimeEntryResponseDto>
- Ordered by EntryDate descending
- Auth: Required, user must have access to task

**DELETE /api/timeentries/{id}** (AC: 6)
- Deletes specific time entry
- Response: 204 No Content
- Auth: Required, only entry creator can delete

**GET /api/tasks** (Updated for AC: 8)
- Existing endpoint now includes TotalLoggedMinutes in TaskResponseDto
- Calculated by summing TimeEntries.Minutes for each task

### Authorization Rules (AC: 9)

1. **Creating Time Entries:**
   - Users can only create time entries for themselves
   - userId parameter extracted from JWT token (not from request body)
   - Service validates userId matches authenticated user

2. **Deleting Time Entries:**
   - Only the entry creator (UserId) can delete their own entry
   - Attempting to delete another user's entry returns 403 Forbidden
   - Service layer enforces this rule before deletion

3. **Viewing Time Entries:**
   - Users can view time entries for tasks they have access to
   - Access determined by task ownership or assignment

### Validation Rules (AC: 10)

**Minutes Field:**
- Must be > 0 (positive value)
- Must be <= 1440 (24 hours maximum)
- Enforced at three levels:
  1. DataAnnotations on DTO: `[Range(1, 1440)]`
  2. Service layer validation
  3. Database check constraint

**Note Field:**
- Optional (nullable)
- Maximum 500 characters
- Enforced by: `[MaxLength(500)]` and database column type

**EntryType:**
- Required enum value (Timer or Manual)
- Stored as int in database (0 or 1)

### Dependency Updates

**Task Entity Changes (AC: 7):**
- Add navigation property: `ICollection<TimeEntry> TimeEntries`
- Computed property for frontend: `int TotalLoggedMinutes`
- EF Core tracks relationship via foreign key

**TaskResponseDto Changes (AC: 8):**
- Add property: `TotalLoggedMinutes` (int)
- Calculated by summing TimeEntries.Minutes during mapping
- Included in all task list and detail responses

### Testing

**Unit Test Location:** `TaskFlow.Tests/Unit/Services/TimeEntryServiceTests.cs`

**Integration Test Location:** `TaskFlow.Tests/Integration/Controllers/TimeTrackingControllerTests.cs`

**Test Coverage Required:**
- Service methods (LogTimeAsync, GetTaskTimeEntriesAsync, DeleteTimeEntryAsync)
- Authorization logic (AC: 9)
- Validation logic (AC: 10)
- API endpoints (AC: 4, 5, 6, 8)
- Multiple entries per task (AC: 13)

**Testing Frameworks:**
- xUnit for test framework
- Moq for mocking dependencies
- FluentAssertions for readable assertions
- WebApplicationFactory for integration tests

**Test Naming Convention:**
- C#: `MethodName_ShouldDoSomething_WhenCondition()`
- Example: `LogTimeAsync_ShouldThrowValidationException_WhenMinutesExceeds24Hours()`

**Test Structure:**
- Arrange: Set up mocks, test data, expected values
- Act: Execute the method under test
- Assert: Verify expected behavior using FluentAssertions

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-29 | 1.0 | Initial story creation | Sarah (PO Agent) |

## Dev Agent Record

### Agent Model Used

(To be populated by dev agent)

### Debug Log References

(To be populated by dev agent)

### Completion Notes List

(To be populated by dev agent)

### File List

(To be populated by dev agent)

## QA Results

(To be populated by QA agent)
