# Story 2.10: Time Logs View — Full-Stack Implementation

## Status

Approved

## Story

**As a** freelancer tracking billable work,
**I want** a dedicated Time Logs page showing all time entries across tasks with filtering, summary stats, and CSV export,
**so that** I can review, audit, and report on my tracked hours efficiently.

## Design Reference

- **Figma file:** [TaskFlow — Time Tracking Logs](https://www.figma.com/design/4CgRl2E328aPKmz0Fd1Q7Q/TaskFlow?node-id=7-1526)
- **Node:** `7:1526` (`Body` frame of "Time tracking - logs" page)

## Figma Design Summary

The Figma design (node `7:1526`) specifies a time logs page with the following layout inside the shared app shell (shellbar + sidebar already handled by `app.html`):

### Page Heading Row

- **Title:** "Time Logs" (`h2`, Manrope 700 / 24px / `#111418`).
- **Right-aligned action buttons:**
  - Secondary: "Export CSV" (white bg, `1px solid #E2E8F0`, shadow, `border-radius: 8px`, icon + text).
  - Primary: blue bg (`#137FEC`), white text, `border-radius: 8px` — action button (likely "Log Time" / "New Entry").

### Stats Cards Row (3 cards, gap 24px)

Three summary stat cards, each 304px wide, white bg, `1px solid #E2E8F0`, `box-shadow: 0px 1px 2px rgba(0,0,0,0.05)`, `border-radius: 12px`, `padding: 24px`.

| Card | Label | Sample Value | Badge | Badge Colour |
|------|-------|-------------|-------|-------------|
| 1 | Total Hours This Week | 42h 15m | +5.2% | Green (`#DCFCE7` bg, `#166534` text) |
| 2 | Billable Hours | 35h 00m | +2.1% | Green |
| 3 | Non-Billable Hours | 7h 15m | -1.5% | Red (`#FEE2E2` bg, `#991B1B` text) |

Each card has a small icon with a blue-tinted background circle on the left side.

### Filter / Search Bar

White card container, `1px solid #E2E8F0`, shadow, `border-radius: 12px`, `padding: 16px`, `justify-content: space-between`.

**Left side (horizontal, gap 12px):**
1. **Date range picker** — button with calendar icon + "This Week" text + dropdown chevron, light bg, border, `border-radius: 8px`.
2. **Vertical divider** — `1px` wide, `#E2E8F0`.
3. **"All Users"** filter dropdown — icon + text + dropdown chevron.
4. **"Project"** filter dropdown — icon + text + dropdown chevron.
5. **"More"** filter dropdown — icon + text + dropdown chevron (extensibility point for additional filters).

**Right side:**
- Search input and/or sort/view toggle controls.

### Data Table

White card container, same border/shadow/radius as filter bar.

**Column Headers** (gray bg `#F8FAFC`, uppercase, Manrope 700 / 12px / `#64748B`, 5% letter-spacing):

| Column | Content | Notes |
|--------|---------|-------|
| Checkbox | Bulk select | Selection checkbox |
| DATE | Date + time | Primary: "Today" / formatted date (Manrope 500 / 14px), Secondary: time (12px) |
| TASK / PROJECT | Two-line cell | Primary: task name, Secondary: project name (Manrope 400 / 12px / `#64748B`) |
| DESCRIPTION | Note text | Max-width 200–270px, text truncated/clipped |
| TIME | Duration badge | e.g. "4h 30m" in a light pill/badge (`border-radius: 6px`, light bg, border) |
| BILLABLE | Icon indicator | Checkmark icon for billable entries |
| ACTIONS | Action buttons | 3 icon buttons (edit, delete, more) — `opacity: 0` default, show on row hover |

**Rows:** 5 visible rows with `-1px` item spacing (overlapping borders for continuous lines). Row height ~89px.

### Pagination Footer

Gray bg (`#F8FAFC`), top border, `padding: 16px 24px`, `justify-content: space-between`.
- **Left:** "Showing X of Y entries" text.
- **Right:** Page number navigation controls.

### Key Design Tokens

| Token | Value | Usage |
|-------|-------|-------|
| Brand blue | `#137FEC` | Primary button, icon tints |
| Card border | `#E2E8F0` | All card/table borders |
| Card shadow | `0 1px 2px rgba(0,0,0,0.05)` | Subtle elevation |
| Card radius | `12px` | Cards; `8px` for buttons |
| Text primary | `#111418` | Headings, entry values |
| Text secondary | `#64748B` | Labels, subtitles |
| Header bg | `#F8FAFC` | Table header, pagination |
| Green badge bg | `#DCFCE7` | Positive change badges |
| Green badge text | `#166534` | Positive change text |
| Red badge bg | `#FEE2E2` | Negative change badges |
| Red badge text | `#991B1B` | Negative change text |
| Time pill bg | `#F1F5F9` | Duration badge background |
| Font: headings | Manrope 700 | Titles, table headers |
| Font: body | Manrope 400–500 | Body text, cell values |

## Backend Gap Analysis

The existing backend has a TimeEntry entity, service, and a partially-implemented controller (DELETE only). The following **new backend work** is required to support this view:

### Existing Backend Assets

- **Entity:** `TimeEntry` (Id, TaskId, UserId, Minutes, EntryDate, Note, EntryType, CreatedAt)
- **DTOs:** `TimeEntryCreateDto`, `TimeEntryResponseDto` (with UserName), `PaginatedResultDto<T>`
- **Service:** `ITimeEntryService` with `LogTimeAsync`, `GetTaskTimeEntriesAsync`, `DeleteTimeEntryAsync`
- **Repository:** `ITimeEntryRepository` with CRUD + `GetTotalMinutesByTaskIdAsync`
- **Controller:** `TimeEntriesController` — only DELETE endpoint implemented
- **Route:** `/logs` route and sidebar nav entry already exist; `LogsComponent` is a placeholder

### Required Backend Changes

1. **Add `IsBillable` field** to `TimeEntry` entity (bool, default false) + DB migration
2. **Add `IsBillable`** to `TimeEntryCreateDto` and `TimeEntryResponseDto`
3. **Add `TaskName` and `ProjectName`** to `TimeEntryResponseDto` (resolved via Task navigation + parent hierarchy)
4. **New endpoint: `GET /api/timeentries`** — paginated list of all user time entries with query params:
   - `page` (int, default 1)
   - `pageSize` (int, default 10)
   - `startDate` / `endDate` (DateTime, optional — date range filter)
   - `userId` (Guid, optional — admin/team filter; default = current user)
   - `projectId` (Guid, optional — filter by root-level Project task)
   - `isBillable` (bool?, optional)
   - `search` (string, optional — searches Note text)
   - Returns `PaginatedResultDto<TimeEntryResponseDto>`
5. **New endpoint: `GET /api/timeentries/summary`** — aggregated stats:
   - Query params: `startDate`, `endDate`, `userId` (same filters as list)
   - Returns: `{ totalMinutes, billableMinutes, nonBillableMinutes, previousPeriodTotalMinutes, previousPeriodBillableMinutes, previousPeriodNonBillableMinutes }` for percent-change calculation
6. **New endpoint: `GET /api/timeentries/export`** — CSV download with same filter params as list
7. **Complete controller:** Wire POST (create), GET by ID, and PATCH (update) endpoints already defined in the API spec
8. **Repository:** Add `GetPaginatedAsync` with filtering, `GetSummaryAsync` for aggregation, and `GetForExportAsync`
9. **Service:** Add corresponding methods to `ITimeEntryService` and `TimeEntryService`

## Acceptance Criteria

### Backend

1. `TimeEntry` entity extended with `IsBillable` (bool, default false); EF migration created and applied.
2. `GET /api/timeentries` returns a `PaginatedResultDto<TimeEntryResponseDto>` supporting `page`, `pageSize`, `startDate`, `endDate`, `userId`, `projectId`, `isBillable`, and `search` query parameters.
3. `TimeEntryResponseDto` includes `TaskName` (string) and `ProjectName` (string, resolved from nearest ancestor with `TaskType.Project`; empty if none).
4. `TimeEntryResponseDto` includes `IsBillable` (bool).
5. `GET /api/timeentries/summary` returns total, billable, and non-billable minutes for the requested period **and** the equivalent previous period (for percent-change badges).
6. `GET /api/timeentries/export` returns a `text/csv` file with columns: Date, Task, Project, Description, Duration, Billable — filtered by the same query parameters as the list endpoint.
7. `POST /api/timeentries` (create) and `PATCH /api/timeentries/{id}` (update) endpoints are wired in the controller, delegating to existing service methods.
8. Authorization enforced: users can only view/modify their own entries (admin/team filter deferred to a future story).
### Frontend

9. `LogsComponent` replaced with full implementation matching the Figma design.
10. **Page heading** renders "Time Logs" as `h2` with right-aligned "Export CSV" (secondary) and primary action button.
11. **Stats cards row** displays three summary cards (Total Hours This Week, Billable Hours, Non-Billable Hours) with values from the summary endpoint and percent-change badges.
12. **Filter bar** renders date-range picker (defaults to "This Week"), "All Users" dropdown, "Project" dropdown, and "More" dropdown — each filter triggers API re-fetch.
13. **Data table** renders columns: Checkbox, Date, Task/Project, Description, Time, Billable, Actions — matching Figma styling.
14. **Date column** shows relative label ("Today", "Yesterday") or formatted date as primary line, with time as secondary line.
15. **Task/Project column** shows task name (primary) and project name (secondary) in a two-line cell.
16. **Time column** displays formatted duration (e.g. "4h 30m") inside a styled pill/badge.
17. **Billable column** shows a checkmark icon for billable entries, empty for non-billable.
18. **Actions column** shows edit/delete/more icon buttons that appear on row hover (`opacity: 0` → `opacity: 1`).
19. **Pagination footer** shows "Showing X of Y entries" with page navigation controls.
20. **Export CSV** button triggers file download via the export endpoint with current filter state.
21. **Responsive:** Table layout at desktop; simplified card/list at mobile breakpoint.
22. **Existing functionality preserved:** Sidebar "Logs" link continues to work; timer widget and other time tracking features unaffected.

## Tasks / Subtasks

### Phase 1: Backend — Entity & Migration

- [ ] **Task 1: Add `IsBillable` to TimeEntry entity** (AC: 1, 4)
  - [ ] Add `public bool IsBillable { get; set; }` to `TimeEntry.cs` (default false)
  - [ ] Update `TimeEntryConfiguration.cs` with column mapping
  - [ ] Create EF Core migration for the new column
  - [ ] Add `IsBillable` to `TimeEntryCreateDto` (optional, default false)
  - [ ] Add `IsBillable` to `TimeEntryResponseDto`

- [ ] **Task 2: Add `TaskName` and `ProjectName` to response DTO** (AC: 3)
  - [ ] Add `TaskName` (string) to `TimeEntryResponseDto`
  - [ ] Add `ProjectName` (string) to `TimeEntryResponseDto`
  - [ ] Update `MapToResponseDto` in `TimeEntryService` to populate `TaskName` from `TimeEntry.Task.Name`
  - [ ] Implement project name resolution: walk `Task.ParentTask` chain up to find first ancestor with `TaskType.Project`, use its `Name`; empty string if none found

### Phase 2: Backend — Paginated List Endpoint

- [ ] **Task 3: Create `TimeEntryFilterDto`** (AC: 2)
  - [ ] Create `TimeEntryFilterDto.cs` in `DTOs/TimeEntries/`:
    - [ ] `Page` (int, default 1)
    - [ ] `PageSize` (int, default 10, max 100)
    - [ ] `StartDate` (DateTime?, optional)
    - [ ] `EndDate` (DateTime?, optional)
    - [ ] `UserId` (Guid?, optional — defaults to current user)
    - [ ] `ProjectId` (Guid?, optional)
    - [ ] `IsBillable` (bool?, optional)
    - [ ] `Search` (string?, optional — searches Note field)

- [ ] **Task 4: Add paginated query to repository** (AC: 2)
  - [ ] Add `GetPaginatedAsync(TimeEntryFilterDto filter, CancellationToken ct)` to `ITimeEntryRepository`
  - [ ] Implement in `TimeEntryRepository`:
    - [ ] Build `IQueryable<TimeEntry>` with `.Include(te => te.Task).Include(te => te.User)`
    - [ ] Apply filters: date range, userId, projectId (via Task ancestor), isBillable, search (Note contains)
    - [ ] Order by `EntryDate` descending
    - [ ] Apply pagination with `.Skip()` / `.Take()`
    - [ ] Return `PaginatedResultDto<TimeEntry>`

- [ ] **Task 5: Add paginated list to service** (AC: 2)
  - [ ] Add `GetPaginatedTimeEntriesAsync(TimeEntryFilterDto, Guid currentUserId, CancellationToken)` to `ITimeEntryService`
  - [ ] Implement in `TimeEntryService`: call repository, map each entry via `MapToResponseDto` (with task name and project resolution)
  - [ ] Return `PaginatedResultDto<TimeEntryResponseDto>`

- [ ] **Task 6: Wire `GET /api/timeentries` in controller** (AC: 2, 7, 8)
  - [ ] Add `[HttpGet]` action accepting `[FromQuery] TimeEntryFilterDto` in `TimeEntriesController`
  - [ ] Default `filter.UserId` to `GetCurrentUserId()` if not provided
  - [ ] Call service, return `Ok(result)`
  - [ ] Also wire `[HttpPost]` (delegates to existing `LogTimeAsync`) and `[HttpGet("{id:guid}")]` endpoints

### Phase 3: Backend — Summary & Export Endpoints

- [ ] **Task 7: Create summary endpoint** (AC: 5)
  - [ ] Create `TimeEntrySummaryDto.cs`:
    - [ ] `TotalMinutes`, `BillableMinutes`, `NonBillableMinutes`
    - [ ] `PreviousPeriodTotalMinutes`, `PreviousPeriodBillableMinutes`, `PreviousPeriodNonBillableMinutes`
  - [ ] Add `GetSummaryAsync` to `ITimeEntryRepository` and implementation (aggregate query with date math for previous period)
  - [ ] Add `GetSummaryAsync` to `ITimeEntryService`
  - [ ] Add `[HttpGet("summary")]` to controller, accepting `startDate`, `endDate`, `userId` query params

- [ ] **Task 8: Create CSV export endpoint** (AC: 6)
  - [ ] Add `GetForExportAsync` to repository (returns all matching entries, no pagination)
  - [ ] Add `ExportTimeEntriesAsync` to service — builds CSV string with columns: Date, Task, Project, Description, Duration, Billable
  - [ ] Add `[HttpGet("export")]` to controller — returns `File(csvBytes, "text/csv", "time-logs.csv")`

### Phase 4: Frontend — Page Structure & Stats

- [ ] **Task 9: Replace placeholder `LogsComponent` with page scaffold** (AC: 9, 10)
  - [ ] Create full component with standalone imports (CommonModule, Material modules)
  - [ ] Add page heading row: "Time Logs" title + "Export CSV" button + primary action button
  - [ ] Create `TimeLogApiService` in `core/services/api/` for all time log API calls
  - [ ] Create `TimeLogStateService` in `core/services/state/` managing filters, pagination, entries signal/observable

- [ ] **Task 10: Implement stats cards** (AC: 11)
  - [ ] Create `TimeLogStatsComponent` (inline or child component)
  - [ ] Fetch summary from `GET /api/timeentries/summary` using current filter date range
  - [ ] Render 3 cards: Total Hours, Billable Hours, Non-Billable Hours
  - [ ] Format minutes as "Xh Ym"
  - [ ] Calculate percent change: `((current - previous) / previous) * 100`
  - [ ] Style badges: green for positive, red for negative

### Phase 5: Frontend — Filters & Table

- [ ] **Task 11: Implement filter bar** (AC: 12)
  - [ ] Date range picker with presets: "Today", "This Week", "This Month", "Custom"
  - [ ] "All Users" dropdown (populated from users list — or current user only for MVP)
  - [ ] "Project" dropdown (populated from tasks with `TaskType.Project`)
  - [ ] "More" dropdown (extensibility — billable toggle for now)
  - [ ] Each filter change triggers re-fetch with updated `TimeEntryFilterDto`

- [ ] **Task 12: Implement data table** (AC: 13, 14, 15, 16, 17, 18)
  - [ ] Checkbox column with bulk select (header checkbox for select-all)
  - [ ] Date column: relative date ("Today", "Yesterday") or formatted, with time as sub-line
  - [ ] Task/Project column: two-line cell — task name (primary) + project name (secondary)
  - [ ] Description column: truncated note text, max-width ~200–270px
  - [ ] Time column: formatted "Xh Ym" inside styled pill badge
  - [ ] Billable column: checkmark icon for billable, empty for non-billable
  - [ ] Actions column: edit/delete/more icons, `opacity: 0` → visible on hover
  - [ ] Row styling: 89px height, `-1px` margin for border overlap

- [ ] **Task 13: Implement pagination & export** (AC: 19, 20)
  - [ ] Pagination footer: "Showing X of Y entries" + page navigation buttons
  - [ ] Sync pagination state with API `page`/`pageSize` params
  - [ ] "Export CSV" button: call `GET /api/timeentries/export` with current filters, trigger browser file download

### Phase 6: Frontend — Polish

- [ ] **Task 14: Responsive layout** (AC: 21)
  - [ ] Desktop: full table layout per Figma
  - [ ] Mobile: simplified card/list view (similar pattern to task list mobile view)

- [ ] **Task 15: Verify integration & regression** (AC: 22)
  - [ ] Sidebar "Logs" link navigates correctly
  - [ ] Timer widget unaffected
  - [ ] Other time tracking features (manual entry, task time entries) still work

## Dev Notes

- **Priority:** P2
- **Epic:** Epic 2 — Task Organization & Time Tracking
- **Design source:** `https://www.figma.com/design/4CgRl2E328aPKmz0Fd1Q7Q/TaskFlow?node-id=7-1526`
- **Figma reference node:** `7:1526` (`Body` frame of "Time tracking - logs" page)
- **Related stories:**
  - [2.6-time-tracking-backend-data-model.md](2.6-time-tracking-backend-data-model.md) — Original TimeEntry entity/service
  - [2.7-active-timer-ui.md](2.7-active-timer-ui.md) — Timer widget (must not regress)
  - [2.8-manual-time-entry-ui.md](2.8-manual-time-entry-ui.md) — Manual time entry (must not regress)
  - [4.10-task-list-refactor-figma-alignment.md](4.10-task-list-refactor-figma-alignment.md) — Similar Figma-alignment pattern for reference
- **Existing route:** `/logs` already defined in `app.routes.ts`, sidebar nav entry exists in `sidebar.ts`
- **Existing placeholder:** `src/app/features/logs/logs.ts` — replace in-place
- **Backend starting points:**
  - Entity: `backend/TaskFlow.Abstractions/Entities/TimeEntry.cs`
  - Controller: `backend/TaskFlow.Api/Controllers/TimeEntriesController.cs` (DELETE only — needs POST/GET/PATCH)
  - Service: `backend/TaskFlow.Core/Services/TimeEntryService.cs`
  - Repository interface: `backend/TaskFlow.Abstractions/Interfaces/Repositories/ITimeEntryRepository.cs`
- **Project resolution approach:** The `Task` entity has `TaskType` enum (`Project = 0, Milestone = 1, Task = 2`) and `ParentTaskId`. To find the "Project" for a time entry, walk `Task → ParentTask → ...` until finding one with `TaskType == Project`. Use a CTE or recursive include if performance is a concern.
- **Date range "previous period":** For the summary percent-change, if current period is "This Week" (Mon–Sun), previous period is the prior week. Use the same duration offset (e.g. 7 days back for a week filter).
- **IsBillable default:** New field defaults to `false`. Existing time entries will all be non-billable until manually updated — this is acceptable for first release.
- **CSV export** should stream results for large datasets. For MVP, buffered in-memory is acceptable.
- Reuse existing design tokens and component patterns from the task list view (card wrapper, table headers, pagination footer, pill badges).
