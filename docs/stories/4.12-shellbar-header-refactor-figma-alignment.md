# Story 4.12: Shellbar (Header) — Reusable Component Refactor & Figma Alignment

## Status

Approved

## Story

**As a** TaskFlow user,
**I want** a consistent, polished top header bar (shellbar) across all authenticated pages that matches the approved Figma design,
**so that** I always have access to global search, a quick-create action, notifications, and my profile/account controls regardless of which view I'm on.

## Design Reference

- **Figma file:** TaskFlow — Header (Shellbar) via figma_local MCP server
- **Node:** `7:3` (`Header` frame, 1280 × 64px)
- **Appears on:** Every authenticated page alongside the sidebar — My Tasks ListView, Task Full Details, Tree View, Timeline, Time Tracking Logs, etc.

## Figma Design Summary

The Figma design specifies a full-width top header bar (shellbar) that sits above the main content area and spans the full viewport width. It is a horizontal layout with two main zones: a **left zone** (branding + search) and a **right zone** (actions + profile).

### Overall Header Container (`7:3` — "Header")
- **Width:** Full viewport width (fills parent)
- **Height:** 64px fixed
- **Background:** White (`#FFFFFF`)
- **Bottom border:** 1px solid `#E2E8F0` (Slate-200)
- **Layout:** Horizontal, `space-between`, vertically centered children
- **Padding:** 24px left / 24px right

---

### Left Zone (`7:4` — "Container")
**Layout:** Horizontal, vertically centered, hug contents, 24px gap between children

#### Brand Section (`7:5` — "Container")
- **Layout:** Horizontal, vertically centered, 12px gap
- **Logo block (`7:6` — "Background"):**
  - 32 × 32px rounded square (border-radius 8px)
  - Background: `#137FEC` (Brand blue — `rgba(19, 127, 236, 1)`)
  - Contains a white icon (checkmark/task icon, ~16.67px) centered
- **App name (`7:10` — "Text"):**
  - Text: **"TaskFlow"**
  - Font: Manrope Bold (700), 18px, line-height 28px, letter-spacing -0.45px
  - Color: `#111418` (near-black — `rgba(17, 20, 24, 1)`)

#### Search Input (`7:13` — "Input")
- **Width:** 384px fixed
- **Height:** 38px (hugs content vertically)
- **Background:** `#F6F7F8` (Light gray — `rgba(246, 247, 248, 1)`)
- **Border:** 1px solid `#E2E8F0` (Slate-200)
- **Border-radius:** 8px
- **Padding:** 40px left (to accommodate search icon), 12px right, 8px top, 9px bottom
- **Search icon:** Positioned absolutely at 12px left, vertically centered (~15px size, color `#637587`)
- **Placeholder text:** "Search tasks, projects, people..."
  - Font: Manrope Regular (400), 14px, line-height 21px
  - Color: `#637587` (Slate-500)

---

### Right Zone (`7:19` — "Container")
**Layout:** Horizontal, vertically centered, 16px gap between children

#### Primary Action Button (`7:20` — "Button")
- **Background:** `#137FEC` (Brand blue)
- **Border-radius:** 8px
- **Padding:** 16px horizontal, 8px vertical
- **Shadow:** `0 1px 2px rgba(0, 0, 0, 0.05)`
- **Layout:** Horizontal, 8px gap between icon and text, vertically centered
- **Icon:** White plus/add icon (~11.67px)
- **Text:** "Create Task"
  - Font: Manrope Medium (500), 14px, line-height 20px
  - Color: White (`#FFFFFF`)

#### Vertical Divider (`7:25` — "Margin" > `7:26` — "Vertical Divider")
- **Width:** 1px
- **Height:** 24px
- **Color:** `#E2E8F0` (Slate-200)
- **Horizontal margin:** 4px each side (total 9px container width)

#### Notification Bell Button (`7:27` — "Button")
- **Width:** ~29px, Height: ~33px (hug content)
- **Border-radius:** 8px
- **Padding:** 8px all sides
- **Background:** Transparent (hover state can add subtle bg)
- **Icon:** Bell/notification icon (~13.3 × 16.7px), color `#637587` (Slate-500)

#### User Profile Section (`7:31` — "Container")
- **Layout:** Horizontal, vertically centered, 12px gap
- **Border-radius:** 8px
- **Padding:** 6px all sides
- **Avatar (`7:32` — "Image"):**
  - 32 × 32px circle (border-radius 9999px / fully rounded)
  - Contains user profile image (cropped)
- **User Info (`7:33` — "Container"):**
  - **Layout:** Vertical, left-aligned
  - **User name (`7:35` — "Text"):** "Sarah Connor"
    - Font: Manrope Medium (500), 14px, line-height 14px
    - Color: `#111418` (near-black)
    - Text overflow: single line, clip
  - **User role (`7:38` — "Product Manager"):**
    - Font: Manrope Regular (400), 12px, line-height 16px
    - Color: `#637587` (Slate-500)
    - Text overflow: multi-line wrap, max width fills container
  - **Spacing:** 4px gap between name and role (via Margin wrapper `7:36`)
- **Dropdown caret (`7:39` > `7:40` — "Icon"):**
  - Chevron-down icon, ~10 × 6.17px
  - Color: `#637587` (Slate-500)
  - Opens user dropdown menu (logout, profile, etc.)

---

## Acceptance Criteria

1. **Reusable shellbar component** is created at `src/app/shared/components/shellbar/` as a standalone Angular component that can be included in the app shell layout for all authenticated pages.
2. **Shellbar layout** renders as a 64px fixed-height bar at the top of the viewport, spanning the full width (to the right of the sidebar if sidebar is present). It uses `space-between` horizontal layout with 24px horizontal padding.
3. **Brand section** displays the TaskFlow logo (32px blue rounded square with white icon) and "TaskFlow" text in Manrope Bold 18px, with 12px gap between them.
4. **Search input** renders as a 384px-wide, 38px-tall rounded input with light gray background (`#F6F7F8`), slate border, search icon overlay (left), and placeholder "Search tasks, projects, people..." in Manrope Regular 14px. The search input emits a value for future wiring to a search feature.
5. **"Create Task" button** is styled as the primary CTA with brand blue background, white text/icon, 8px border-radius, subtle drop shadow, and triggers navigation to the create-task flow or opens a create-task dialog.
6. **Vertical divider** separates the CTA from the notification/profile section — 1px wide, 24px tall, `#E2E8F0` color.
7. **Notification bell** icon button renders with a bell icon in slate color, with 8px padding and 8px border-radius, clickable placeholder for future notification panel.
8. **User profile section** displays the current user's avatar (32px circle), name (Manrope Medium 14px), and role/title (Manrope Regular 12px) with a dropdown caret icon. Clicking the profile section opens a dropdown menu with at minimum a "Logout" option.
9. **User data** is dynamically sourced from `AuthService.currentUser$` — avatar, display name, and role/title are rendered from the authenticated user model.
10. **Logout functionality** from the existing `NavigationComponent` is fully preserved and accessible from the user profile dropdown in the shellbar.
11. **App shell integration** updates `app.html` to render the shellbar above the sidebar+content area for authenticated views. The layout becomes: `shellbar (top, full-width)` → `sidebar (left) | content (right)`.
12. **The old `NavigationComponent`** (`src/app/shared/components/navigation/`) is removed or deprecated, with all its responsibilities migrated to the shellbar and sidebar.
13. **Responsive behavior:** On viewports below 1024px, the search input collapses (hidden or icon-only), the "Create Task" text may collapse to icon-only, and the user profile section shows only the avatar. The shellbar height remains 64px.
14. **Accessibility:** The shellbar uses a `<header>` landmark with appropriate `role` and `aria-label`. The search input has an associated label. All buttons have `aria-label` attributes. The profile dropdown is keyboard-accessible (Enter/Space to open, Escape to close, arrow keys to navigate).
15. **Design tokens match Figma exactly:** per the Figma Design Summary section above — all colors, spacing, typography, border-radius, and shadow values.
16. **No regression** in existing functionality — sidebar navigation, task list, task details, timeline, auth flows, and timer widget all continue to work correctly.

## Tasks / Subtasks

- [ ] **Task 1: Create Shellbar Component Scaffold** (AC: 1, 15)
  - [ ] Generate `src/app/shared/components/shellbar/shellbar.ts` as a standalone Angular component
  - [ ] Create `shellbar.html` template with `<header>` landmark wrapper
  - [ ] Create `shellbar.css` with base styles: 64px height, white background, bottom border `1px solid #E2E8F0`, horizontal flex with `space-between`, 24px horizontal padding, vertically centered children
  - [ ] Export component for use in app shell

- [ ] **Task 2: Implement Brand Section** (AC: 3, 15)
  - [ ] Create logo element: 32×32px container, `#137FEC` background, 8px border-radius, centered white checkmark icon (use `mat-icon` `task_alt` or SVG)
  - [ ] Add "TaskFlow" text: Manrope Bold 700, 18px, line-height 28px, letter-spacing -0.45px, color `#111418`
  - [ ] Layout: horizontal flex, vertically centered, 12px gap

- [ ] **Task 3: Implement Search Input** (AC: 4, 15)
  - [ ] Create search input container: 384px width, 38px height, `#F6F7F8` background, `1px solid #E2E8F0` border, 8px border-radius
  - [ ] Position search icon absolutely at 12px from left, vertically centered, ~15px size, color `#637587`
  - [ ] Style input: 40px left padding (past icon), 12px right padding, placeholder "Search tasks, projects, people..."
  - [ ] Placeholder font: Manrope Regular 400, 14px, line-height 21px, color `#637587`
  - [ ] Add `(input)` or reactive form control to emit search value for future wiring
  - [ ] Add accessible `aria-label="Search tasks, projects, and people"` to input

- [ ] **Task 4: Implement Right Zone — Create Task Button** (AC: 5, 15)
  - [ ] Style button: `#137FEC` background, 8px border-radius, 16px horizontal / 8px vertical padding, `box-shadow: 0 1px 2px rgba(0,0,0,0.05)`
  - [ ] Add white plus icon (`mat-icon` `add`) ~12px and "Create Task" text: Manrope Medium 500, 14px, line-height 20px, white
  - [ ] Layout: horizontal, 8px gap between icon and text
  - [ ] Wire click event: emit `createTask` output event or call router to navigate to create-task route
  - [ ] Add `aria-label="Create new task"`

- [ ] **Task 5: Implement Right Zone — Divider & Notification Bell** (AC: 6, 7, 15)
  - [ ] Add vertical divider: 1px wide, 24px height, `#E2E8F0` background, within a 9px-wide container (4px padding each side)
  - [ ] Add notification bell button: 8px padding, 8px border-radius, transparent background, bell icon (`mat-icon` `notifications_none`) in `#637587`
  - [ ] Wire click: placeholder `(click)` handler or output event for future notification panel
  - [ ] Add `aria-label="Notifications"` to bell button

- [ ] **Task 6: Implement Right Zone — User Profile Section** (AC: 8, 9, 10, 15)
  - [ ] Create profile container: horizontal flex, 12px gap, 8px border-radius, 6px padding, clickable
  - [ ] Avatar: 32×32px circle image from `currentUser$.avatar` (or fallback initials/icon), border-radius 9999px
  - [ ] User info: vertical layout — name (Manrope Medium 500 14px/14px `#111418`) + role (Manrope Regular 400 12px/16px `#637587`), 4px gap
  - [ ] Dropdown caret: chevron-down icon (`mat-icon` `expand_more`), ~10px, `#637587`
  - [ ] Implement dropdown menu (Angular CDK overlay or `mat-menu`) with "Logout" option calling `AuthService.logout()`
  - [ ] Add `aria-haspopup="true"`, `aria-expanded`, keyboard support (Enter/Space to toggle, Escape to close)

- [ ] **Task 7: Integrate Shellbar into App Shell** (AC: 11, 12, 16)
  - [ ] Update `app.html` to add `<app-shellbar>` above the sidebar+content area, conditionally shown for authenticated routes (reuse `showNavigation` signal)
  - [ ] Update `app.css` to establish layout: shellbar (top, full-width) → row below with sidebar (left) + content (right)
  - [ ] Remove or deprecate the old `NavigationComponent` (move logout/user logic fully to shellbar)
  - [ ] Update `app.ts` imports: add `ShellbarComponent`, remove `NavigationComponent` import if still present
  - [ ] Verify sidebar still renders correctly beside content, below shellbar
  - [ ] Verify timer widget still functions

- [ ] **Task 8: Responsive Behavior** (AC: 13)
  - [ ] At `max-width: 1023px`: hide search input (or collapse to icon-only toggle)
  - [ ] At `max-width: 767px`: collapse "Create Task" to icon-only (hide text), hide user name/role (show avatar only)
  - [ ] Shellbar height remains 64px at all breakpoints
  - [ ] Ensure shellbar doesn't overlap or conflict with sidebar's mobile hamburger toggle
  - [ ] Test layout at 1280px, 1024px, 768px, 375px viewports

- [ ] **Task 9: Accessibility** (AC: 14)
  - [ ] Wrap shellbar in `<header role="banner" aria-label="Application header">`
  - [ ] Search input: visible or `aria-label` label, supports Enter to submit
  - [ ] All icon buttons: `aria-label` (Create Task, Notifications, Profile dropdown)
  - [ ] Profile dropdown: `aria-haspopup="menu"`, `aria-expanded`, keyboard navigable (Tab, Enter/Space, Escape, Arrow keys)
  - [ ] Visible focus indicators on all interactive elements (`:focus-visible` outline)
  - [ ] Verify landmark structure: `<header>` (shellbar) + `<nav>` (sidebar) + `<main>` (content)

- [ ] **Task 10: Testing & Regression** (AC: 16)
  - [ ] Write unit tests for `ShellbarComponent`: renders brand, search input, create button, notification bell, user profile
  - [ ] Test user profile dropdown opens/closes, logout calls `AuthService.logout()`
  - [ ] Test responsive classes applied at breakpoints
  - [ ] Verify task list page renders correctly with new layout (shellbar + sidebar + content)
  - [ ] Verify task details page navigation works
  - [ ] Verify login/register pages render without shellbar or sidebar
  - [ ] Run existing test suites and fix any breakage from layout changes

## Dev Notes

### Current State
- The app currently uses a sidebar (`src/app/shared/components/sidebar/`) for primary navigation (implemented in Story 4.11).
- There is an old `NavigationComponent` (`src/app/shared/components/navigation/`) with a `mat-toolbar` that renders "TaskFlow" branding, user name, and a logout button. This is **no longer imported** in `app.ts` (replaced by sidebar in 4.11), but the component files still exist.
- The current `app.html` layout is: `sidebar | main-content`. This story adds the shellbar above that, making it: `shellbar` (top) → `sidebar | main-content` (below).
- User info and logout are currently in the sidebar bottom section — the shellbar will become the **primary location** for user profile & logout, potentially simplifying the sidebar.

### Figma Context
- The Header (node `7:3`) appears across all authenticated Figma page designs as a persistent top bar.
- It complements the Sidebar (node `7:42`) implemented in Story 4.11, creating the full app shell: shellbar on top + sidebar on left + content in the remaining area.
- The Figma layout hierarchy on authenticated pages is: `Body → Header (7:3) → Container → [Aside (sidebar) | Main (content)]`.

### Design Tokens Summary

| Token | Value |
|---|---|
| Shellbar height | 64px |
| Background | `#FFFFFF` |
| Bottom border | 1px solid `#E2E8F0` |
| Horizontal padding | 24px |
| Brand logo size | 32 × 32px |
| Brand logo bg | `#137FEC` |
| Brand logo radius | 8px |
| App name font | Manrope Bold 700, 18px / 28px, -0.45px tracking |
| App name color | `#111418` |
| Brand to search gap | 24px |
| Search input width | 384px |
| Search input height | 38px |
| Search input bg | `#F6F7F8` |
| Search input border | 1px solid `#E2E8F0` |
| Search input radius | 8px |
| Search icon color | `#637587` |
| Placeholder font | Manrope Regular 400, 14px / 21px |
| Placeholder color | `#637587` |
| Right zone gap | 16px |
| CTA button bg | `#137FEC` |
| CTA button radius | 8px |
| CTA button shadow | `0 1px 2px rgba(0,0,0,0.05)` |
| CTA button padding | 16px H / 8px V |
| CTA text font | Manrope Medium 500, 14px / 20px |
| CTA text color | `#FFFFFF` |
| CTA icon-text gap | 8px |
| Divider width | 1px |
| Divider height | 24px |
| Divider color | `#E2E8F0` |
| Bell icon color | `#637587` |
| Bell button padding | 8px, radius 8px |
| Profile container padding | 6px, radius 8px |
| Avatar size | 32 × 32px (circle) |
| User name font | Manrope Medium 500, 14px / 14px |
| User name color | `#111418` |
| User role font | Manrope Regular 400, 12px / 16px |
| User role color | `#637587` |
| Name-to-role gap | 4px |
| Profile info gap | 12px |
| Caret icon color | `#637587` |

### Dependencies
- Story 4.11 (Sidebar Navigation) — sidebar must be in place; shellbar integrates above it
- Story 1.3 (Auth Frontend) — `AuthService`, `currentUser$`, `logout()` method
- Story 1.5 / 1.6 (Task Management) — "Create Task" CTA may navigate to create-task route or open dialog
- Manrope font loaded in `index.html` (added in Story 4.9)

### Priority
- **P1** — The shellbar is a global layout component visible on every authenticated screen. It provides critical UX functionality (search, quick-create, profile/logout) and must be implemented as a reusable shell element to avoid duplicating header logic across pages.

## File List

| File | Action | Description |
|------|--------|-------------|
| `src/app/shared/components/shellbar/shellbar.ts` | Create | Standalone shellbar component with search, CTA, profile dropdown |
| `src/app/shared/components/shellbar/shellbar.html` | Create | Shellbar template with header landmark, left/right zones |
| `src/app/shared/components/shellbar/shellbar.css` | Create | All Figma design tokens, responsive breakpoints |
| `src/app/shared/components/shellbar/shellbar.spec.ts` | Create | Unit tests for shellbar rendering and interactions |
| `src/app/app.ts` | Modify | Add ShellbarComponent import, update shell layout |
| `src/app/app.html` | Modify | Add `<app-shellbar>` above sidebar+content row |
| `src/app/app.css` | Modify | Update layout to shellbar (top) → sidebar + content (below) |
| `src/app/shared/components/navigation/navigation.ts` | Remove/Deprecate | Old top toolbar — functionality moved to shellbar |
| `src/app/shared/components/navigation/navigation.html` | Remove/Deprecate | Old top toolbar template |
| `src/app/shared/components/navigation/navigation.css` | Remove/Deprecate | Old top toolbar styles |
| `src/app/shared/components/sidebar/sidebar.ts` | Modify | Potentially remove user info/logout section (now in shellbar) |
| `src/app/shared/components/sidebar/sidebar.html` | Modify | Potentially remove user info/logout section (now in shellbar) |

## Change Log

| Date | Change | Author |
|------|--------|--------|
| 2026-03-08 | Created story from Figma node 7:3 header/shellbar design specification. | Sally (UX Expert) |
